&После("ПриЗаписи")
Процедура АБ_ПриЗаписи(Отказ, Замещение)
	//++dar 12.03.26 (309302)
	Если ЗначениеЗаполнено(ЭтотОбъект.Отбор.Файл.Значение) Тогда
		УзелЕРП = АБ_ВызовСервера.ЗначениеКонстантыИзРегистра(Новый Структура("Ключ", "УВМ_ПланОбменаСЕРП"));
		ПланыОбмена.ЗарегистрироватьИзменения(УзелЕРП, ЭтотОбъект.Отбор.Файл.Значение);
	КонецЕсли;
	//--dar 12.03.26 (309302)
КонецПроцедуры
